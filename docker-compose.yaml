version: '2'
services:
  web:
    build: .
    restart: always
    ports:
      - "5000:80"
    environment:
      - PORT=80
    volumes:
    - ./user:/var/www/html/user
  sass:
    build: sass
    volumes:
    - ./user:/var/www/html/user
volumes:
  user:
    external: false