enabled: true     # Plugin enabled
active: true     # TOC processed by default for all page
start: 2         # Start header tag level (1 = h1)
depth: 6          # Depth from start (2 = 2 levels deep)
anchors:
  slug_maxlen: 100
